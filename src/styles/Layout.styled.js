import styled from "styled-components";

const gap = "10px";

export const BarCont = styled.div`
  display: flex;
  gap: ${gap};
  padding: ${gap};
  border: 1px solid black;
`;

export const PatternCont = styled.div`
  display: flex;
  justify-content: center;
  gap: ${gap};
`;
